apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: argocd-route
spec:
  http:
    - match:
        paths:
          - /*
      backends:
        - serviceName: argocd-server
          servicePort: 443
